package euler;

/**
 * 
 * @author Sean
 */
public class Euler017 {
    
    public static void main(String[] args) {
        
        
    }
    
    public static int sumOfLettersBetween(int first, int last) {
        if(first > last) System.exit(-1);
        if(first < 1) System.exit(-1);
        if(last > 1000) System.exit(-1);
        
        int sum = 0;
        
        for(int i = first; i <= last; i++) {
            if(i/100 > 0)
        }
        
        return 1;
    }
}
